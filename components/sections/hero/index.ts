/**
 * Hero Section
 *
 * Modular server-rendered hero composed from:
 *   - HeroBackground (galaxy + globe layers)
 *   - HeroNav (siteConfig-driven navigation)
 *   - HeroContent (configurable badge/headline/CTA)
 *   - ClientLogos (reusable trust strip)
 */
export { Hero } from "./hero";
export { HeroBackground } from "./hero-background";
export { HeroNav } from "./hero-nav";
export { HeroContent } from "./hero-content";
export { ClientLogos } from "./client-logos";
